// NOTE: Names are used for tracking, if a name is changed, make sure to
// explicitly use the old name in IdentifyForTracking usecase.
enum ColltactTab {
  contacts,
  sharedContact,
  colleagues,
}
